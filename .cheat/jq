# Pretty print the json
jq "." < filename.json

# Access the value at key "foo"
jq '.foo'

# Access first list item
jq '.[0]'

# Slice & Dice
jq '.[2:4]'
jq '.[:3]'
jq '.[-2:]'

# get minimum value 
jq -s min nums.txt

# get max value 
jq -s min nums.txt

# get avg value 
jq -s add/length nums.txt
